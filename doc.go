/*
fsconvert is a collecion of functions to convert from and
to Go's filesystem interface, defined in io/fs.

It uses testing/fstest for the filesystems it creates, so it is only
suitable for very small filesystems.
*/
package fsconvert
